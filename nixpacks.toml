[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm install", "cd server && npm install"]

[phases.build]
cmds = ["cd server && npm run build"]

[start]
cmd = "node server/dist/index.js"
